<div class="flex">
    <input type="checkbox" class="shrink-0 mt-0.5 border-gray-200 rounded-sm text-primary focus:ring-primary checked:border-primary disabled:opacity-50 disabled:pointer-events-none " id="{{ $name }}" name="{{ $name }}" value="{{ $value }}" {{ $attributes }}>
    <label for="{{ $name }}" class="text-sm text-gray-500 ms-3 ">{{ $label }}</label>
  </div>